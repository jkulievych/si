{% include 'default/_form.html.twig' with {
    page_title: 'Edit news'|trans({'%id%': news.id|default('')}),
    form: form,
    submit_label: 'Edit'|trans,
    back_to_list_path: 'news_index',
} %}