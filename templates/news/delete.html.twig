{% include 'default/_form.html.twig' with {
    page_title: 'Delete news'|trans({'%id%': news.id|default('')}),
    form: form,
    submit_label: 'Delete'|trans,
    back_to_list_path: 'news_index',
} %}
