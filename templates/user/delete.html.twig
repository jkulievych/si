{% include 'default/_form.html.twig' with {
    page_title: 'Delete user'|trans({'%id%': user.id|default('')}),
    form: form,
    submit_label: 'Delete'|trans,
    back_to_list_path: 'user_index',
} %}
