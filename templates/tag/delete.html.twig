{% include 'default/_form.html.twig' with {
    page_title: 'Delete tag'|trans({'%id%': tag.id|default('')}),
    form: form,
    submit_label: 'Delete'|trans,
    back_to_list_path: 'tag_index',
} %}
